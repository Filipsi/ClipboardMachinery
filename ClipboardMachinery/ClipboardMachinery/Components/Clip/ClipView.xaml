<UserControl
    x:Class="ClipboardMachinery.Components.Clip.ClipView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:cal="http://www.caliburnproject.org"
    mc:Ignorable="d" d:DesignWidth="512"
    Background="{StaticResource BodyBackgroundBrush}"
    UseLayoutRounding="True"
    RenderOptions.BitmapScalingMode="NearestNeighbor"
    SnapsToDevicePixels="True"
    RenderOptions.ClearTypeHint="Enabled"
    Loaded="OnLoaded"
    cal:Message.Attach="[Event MouseEnter] = [Action Focus()]; [Event MouseLeave] = [Action Unfocus()]">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            x:Name="TagPanel"
            Grid.Row="0"
            Height="1"
            BorderThickness="0,1,0,0"
            BorderBrush="#E0E0E0"
            Background="#f3f1ef">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ItemsControl Grid.Row="0" ItemsSource="{Binding Tags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel VerticalAlignment="Stretch" Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentControl cal:View.Model="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock
                    Grid.Column="1"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Foreground="#2e3131"
                    FontSize="14"
                    FontFamily="Yu Gothic UI Light"
                    Text="{Binding Model.Created, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}" />
            </Grid>
        </Border>

        <!-- Clip grid -->
        <Grid
            Grid.Row="1"
            Margin="0,4,0,4">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="24" />
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <Path
                Grid.Column="0"
                Margin="8,0"
                Data="{Binding Icon, FallbackValue={StaticResource IconTextFile}}"
                Fill="{Binding SelectionColor, FallbackValue={StaticResource BodyControlBrush}}"
                Stretch="Uniform" />

            <!-- Main content -->
            <Grid
                Grid.Column="1"
                cal:Message.Attach="[Event MouseLeftButtonUp] = [Action Select()]"
                Background="Transparent">

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ContentControl
                    Grid.Row="0"
                    Content="{Binding Content}"
                    Foreground="Black"
                    FontSize="14" />
            </Grid>

            <!-- Controls -->
            <ItemsControl Grid.Column="2" x:Name="Controls">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid
                            Background="Transparent"
                            Cursor="Hand"
                            Margin="5"
                            cal:Bind.Model="{Binding}"
                            cal:Message.Attach="
                                [Event MouseLeftButtonUp] = [Action InvokeClickAction()];
                                [Event MouseEnter] = [Action Focus()];
                                [Event MouseLeave] = [Action Unfocus()]">

                            <Path Data="{Binding Icon}" Fill="{Binding Color}" Stretch="Uniform" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

    </Grid>
</UserControl>
